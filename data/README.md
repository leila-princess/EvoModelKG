# Data directory

`splits/` contains the immutable 15,000-ID candidate pool, validation IDs, held-out-test IDs, evolution pool, and the 18 fixed generation batches. `split_protocol.json` records the exact index-based partitioning. `annotations/` contains the located corrected annotation export.

Raw README files, full structured metadata, and private database exports are deliberately excluded.
